# ~/.bashrc: executed by bash(1) for non-login shells.
# bash startup debugging
STARTUP="${STARTUP}~/.bashrc, "

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

export TOOLS=~/Tools
include env
include config
include aliases
include functions
